--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1
einops
transnetv2-pytorch
transformers
sentencepiece
numpy

# OmniShotCut (vendored in OmniShotCut/) — deliberately not installed from its own
# repo, whose requirements.txt pins transformers==4.57.3 and would downgrade the
# version the DINOv3 validation relies on.
ffmpeg-python
huggingface_hub

# pip install opencv-contrib-python with CUDA from here: https://github.com/cudawarped/opencv-python-cuda-wheels/releases
